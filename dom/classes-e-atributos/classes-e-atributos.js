// Adicione a classe ativo a todos os itens do menu

const addClase = document.querySelector('.menu a')

addClase.forEach(item => {
    item.classList.add('ativo')
});

// Remove a clase ativo de todos os itens do menu e mantenha apenas o primeiro

// Verifique se as imagens possuem o atributo alt



// Modifique o href do link exrerno no menu

